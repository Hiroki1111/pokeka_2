import 'package:flutter/material.dart';

class ViewGraph extends StatelessWidget {
  const ViewGraph({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('戦績グラフ'),
      ),
    );
  }
}
